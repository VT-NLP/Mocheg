package checker;

public abstract class AbstractChecker implements Checker{

    /**
     * abstrct class, the classes for checking the function to check urls on web archive implements the functions in it.
     * @param checkUrl
     */

    public void checkUrl(String checkUrl){
        return;
    }

    public void checkLink(String checkUrl,String snopesUrl){
        return;
    }
}
